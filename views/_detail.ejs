<h4 class="m1-2 my-3" style="text-align : center">기록 페이지</h4>
<div class="container mt-2">
<ul class="list-group">
    <li style="background-color: #ddf3c0;" class="list-group-item">
      <!-- 글마다 번호를 부여하여, 식별에 간편함을 추가했다. -->
      <p>No.<%= detail_data._id %></p> 
      <h4>제목 : <%= detail_data.title %></h4> 
      <h4>세부 정보 : <%= detail_data.writes %></h4>
        <a  class="btn btn-warning" data-id="<%= detail_data._id %>" data-toggle="modal" data-target="#exampleModal">수정하기</a>
      <!-- 버튼에도 for문을 활용하여 data-id라는 값에 글의 id를 할당한다. -->
      <!-- data-원하는이름 의 형태로 활용 가능 -->
      <button id = "delete" class="btn btn-warning" data-id="<%= detail_data._id %>" style="float: right">삭제</button></br>
    </li>
  </ul>
</div>

<!-- Button trigger modal -->

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <h4 class="m1-2 my-3" style="text-align : center">기록 페이지</h4>
      <div class="container mt-2">
        <form action="/edit?_method=PUT" method="POST">
            <div class="form-group">
              <label for="exampleFormControlInput1">제목</label>
              <!-- value에 detail_data에 있는 _id를 넣는다. -->
              <!-- id는 변하지 않는 값이기 때문에 id로 데이터를 식별해야한다. -->
              <!-- 그렇기 때문에 form 태그 안에 있는 input 태그에 detail_data._id (해당 데이터의 id)를 숨기고, 서버에 전송을 한다. -->
              <input type="text" value="<%= detail_data._id %>" name="id" style="display: none;">
              
              <!-- value 값에 detail_data.title을 입력하면, input 태그 안에 detail_data.title이 작성이 된 상태로 페이지에 들어갈 수 있다. -->
              <input value="<%= detail_data.title %>" type="text" class="form-control" name="title"  required/>
            </div>
            <div class="form-group">
              <label for="exampleFormControldetail_data1">추가 정보</label>
              <textarea type="text" class="form-control" name="date" size=10  required/><%= detail_data.writes %></textarea>
            </div>
            <button type="submit" class="btn btn-warning">저장</button>
          </form>
        </div>
      <div class="modal-footer">
        
      </div>
    </div>
  </div>
</div>


