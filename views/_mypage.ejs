<h4 class="m1-2 my-3" style="text-align : center">마이페이지</h4>
<div class="jumbotron jumbotron-fluid">
    <div class="container">
      <h1 class="display-4"><%= User_data.id %>님의 마이 페이지입니다</h1>
    </div>
  </div>
  <div class="jumbotron jumbotron-fluid">
    <div class="container">
  <p class="lead">ID : <%= User_data.id %> <button style="display: block; float: right" type="button" class="btn btn-warning" data-toggle="modal" data-target="#exampleModal">회원정보 수정</button> </p>
  <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
            <p>회원정보 수정</p>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <h4 class="m1-2 my-3" style="text-align : center">변경 후에는 다시 로그인을 해야합니다</h4>
        <div class="container mt-2">
          <form action="/change?_method=PUT" method="POST">
              <div class="form-group">
                <label for="exampleFormControlInput1">아이디</label>
                <input type="text" value="<%= User_data._id %>" name="Cid" style="display: none;">
                <input value="<%= User_data.id %>" type="text" class="form-control" name="id_change">
              </div>
              <div class="form-group">
                <label for="exampleFormControlInput1">비밀번호</label>
                <input value="<%= User_data.pw %>" type="text" class="form-control" name="pw_change">
              </div>
              <button type="submit" style="display: block; float: right" class="btn btn-warning">저장</button>
              <button type="button" class="btn btn-secondary" data-dismiss="modal">닫기</button>
          </form>
        </div>
        <div class="modal-footer">
        </div>
      </div>
    </div>
  </div>
      
  <p class="lead">PW : <%= User_data.pw %></p>
    
   